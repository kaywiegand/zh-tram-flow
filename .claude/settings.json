{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // \"\"' | { read -r f; echo \"$f\" | grep -qE 'analytics/.*\\.py$|visualization/.*\\.py$' && cd /Users/kaywiegand/Workspace/zh-tram-flow && source .venv/bin/activate 2>/dev/null && result=$(python scripts/check_style.py 2>&1); rc=$?; if [ $rc -ne 0 ]; then echo \"{\\\"systemMessage\\\": \\\"⚠️ Style violations found:\\\\n$result\\\"}\"; fi; } 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "Checking chart style..."
          }
        ]
      }
    ]
  }
}
